# CrossServerTP sample config - smp_01
# Official paired layout:
#   smp_01 center-x = 30000.5, center-z = 0.5, radius = 30000
#   X range = 0.5 -> 60000.5
#   EAST border at X=60000 sends players to smp_02

server-name: "smp_01"

redis:
  enabled: true
  host: "5.9.152.50"
  port: 6379
  password: ""
  database: 0
  key-prefix: "crossservertp:"
  async-threads: 2
  async-queue-capacity: 256
  status-cache-ttl-ms: 3000
  transfer-timeout-ms: 3000
  pool:
    max-total: 8
    max-idle: 8
    min-idle: 1
  timeout:
    connection: 2000
  reconnect:
    max-retries: 2
    initial-delay-ms: 250

check-interval-ticks: 20
unsafe-destination-block-clearing:
  enabled: false
particle-view-distance: 50
particle-type: FLAME

borders:
  - world: "world"
    side: "EAST"
    coordinate: 60000
    target-server: "smp_02"
    target-world: "world"
    target-side: "WEST"
    target-coordinate: 60000
    target-offset: 5
    show-particles: true

entity-teleportation:
  enabled: true
  teleport-vehicles: true
  teleport-leashed-mobs: true
  teleport-nearby-villagers: true
  nearby-entity-distance: 3
  max-entities: 10
  mobs_must_be_leashed: false
  mobs:
    exclusion: true
    list:
      - WITHER
      - ENDER_DRAGON
  vehicles:
    exclusion: true
    list: []

messages:
  server-offline: "&cThe destination server is currently offline. Please try again later."
